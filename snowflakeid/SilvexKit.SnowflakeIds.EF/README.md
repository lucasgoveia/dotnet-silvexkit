# SilvexKit.SnowflakeIds.EF

Entity Framework Core converter package for `SnowflakeId`.

## Example

```csharp
using Microsoft.EntityFrameworkCore;
using SilvexKit.SnowflakeIds.EF;

// modelBuilder.Entity<MyEntity>()
//     .Property(x => x.Id)
//     .HasConversion(new SnowflakeIdValueConverter());
```
